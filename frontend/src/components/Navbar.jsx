function Navbar() {
  return (
    <div
      style={{
        background: "#1e293b",
        padding: "20px",
        borderRadius: "15px",
        color: "white",
        marginBottom: "25px",
      }}
    >
      <h2>Welcome to Job Portal</h2>
    </div>
  );
}

export default Navbar;